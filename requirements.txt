# ─── Núcleo compartido (todas las plataformas) ───────────────────────────
# Versiones mínimas verificadas en Apple Silicon (Python 3.13).
fastapi>=0.115
uvicorn[standard]>=0.34
websockets>=14.0
pydantic>=2.10
pyyaml>=6.0
python-dotenv>=1.0
openai>=1.59            # cliente compatible con DeepSeek (deepseek-chat)
numpy>=2.0
sounddevice>=0.5        # captura/reproducción de audio nativa
soundfile>=0.13
pywebview>=5.3          # ventana de escritorio ligera

# ─── VAD (detección de voz, común a todas las plataformas) ───────────────
# silero-vad se carga vía torch.hub en backend/voice/vad.py (requiere torchaudio).
torch>=2.5
torchaudio>=2.5

# ─── STT / TTS específicos por plataforma ────────────────────────────────
# NO se instalan aquí. Cada plataforma instala lo suyo:
#   macOS (Apple Silicon):  pip install -r requirements-mac.txt
#   NVIDIA/AMD:             pip install -r requirements-cuda.txt
